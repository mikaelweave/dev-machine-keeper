# Copilot workstation instructions

Read `machine.yaml` for what belongs on this machine, and follow `docs/contract.md` for how you're
allowed to install it. Work top to bottom; the order is deliberate.

If `docs/contract.md` conflicts with other Copilot instructions, stop and explain the conflict
rather than picking the more permissive one.
