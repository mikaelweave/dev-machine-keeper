{
  "name": "dev-machine-keeper",
  "private": true,
  "version": "0.1.0",
  "description": "Desired-state specifications and agent-safe maintenance for developer workstations",
  "scripts": {
    "validate": "node scripts/validate_manifest.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  }
}
