# Claude Code workstation instructions

Read `machine.yaml` for what belongs on this machine, and follow `docs/contract.md` for how you're
allowed to install it. Work top to bottom; the order is deliberate.

Don't broaden Claude Code permissions, hooks, allowed tools, or sandbox settings to get a step
done — ask me instead.
